import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class HTPkeys here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class HTPkeys extends Actor
{
    /**
     * Act - do whatever the HTPkeys wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public void act() 
    {
        GreenfootImage image = getImage();
        image.scale(260, 560);
        setImage(image);
        //setLocation(515, 560); 
        // Add your action code here.
    } 
}
